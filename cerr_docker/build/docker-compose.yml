version: "3"
services:
  cdcs:
    image: ce:${PROJECT_VERSION}
    build:
      context: ./cdcs
      args:
        - CDCS_REPO=${CDCS_REPO}
        - BRANCH=${BRANCH}
        - PROJECT_NAME=${PROJECT_NAME}
        - PIP_CONF=${PIP_CONF}
        - PYTHON_VERSION=${PYTHON_VERSION}
